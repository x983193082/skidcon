# Installation
## 系统依赖
### Linux (Debian/Ubuntu)
```bash
sudo apt-get update
sudo apt-get install nmap
macOS
brew install nmap
Windows
1. 下载 nmap：https://nmap.org/download.html
2. 安装并添加到 PATH
Python 依赖
pip install crewai pydantic
验证安装
nmap --version
预期输出：
Nmap version 7.x.x

---